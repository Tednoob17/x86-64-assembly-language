# Quizzes

This directory contains quiz sources and examples for the interactive book.

- `examples/` shows the TOML structure expected by the quiz system.
- Chapter quiz text files such as `02.txt` contain the question-and-answer
  prompts used while drafting exercises.

Each answer line starts with `R:`.
